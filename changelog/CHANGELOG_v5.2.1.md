# v5.2.1

## Fixes
- Reset mod state when returning to main menu (prevents silent crash on reload)
- Use reflection for private temp_boGrav field in UpdateShip Postfix
- Disable queue-stack patches (movement still queued incorrectly)
